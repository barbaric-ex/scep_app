<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#"><i class="bi bi-amd"></i> School</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if (user) { %>
        <li class="nav-item">
          <a class="nav-link <%=(title==="Students") ? 'active' : ''  %>" aria-current="page" href="/students"
            >Students</a
          >
        </li>
        <% } %>
        <% if (user) { %>
        <li class="nav-item">
          <a class="nav-link <%=(title==="Courses") ? 'active' : ''  %>" href="/courses">Courses</a>
        </li>
        <% } %>

        <% if (user) { %>
        <li class="nav-item">
          <a class="nav-link <%=(title==="Payments") ? 'active' : ''  %>" href="/payments">Payments</a>
        </li>
        <% } %>
        <% if (user) { %>
          <li class="nav-item">
            <a class="nav-link btn btn-danger" href="/logout">Logout</a>
          </li>
        <% } %>
       
        
      </ul>
     
    </div>
  </div>
</nav>
